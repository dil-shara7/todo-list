version: '3.8'
services:
  todo-app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    #health checks
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001"]
      interval: 30s
      timeout: 10s
      retries: 3